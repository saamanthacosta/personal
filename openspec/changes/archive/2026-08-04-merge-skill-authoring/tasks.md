## 1. OpenSpec artifacts

- [ ] 1.1 Validate `openspec validate merge-skill-authoring` (proposal, design, specs/tasks).
- [ ] 1.2 Sync the `skill-authoring` canonical spec delta into `openspec/specs/skill-authoring/spec.md` after the apply phase.

## 2. Author `skill-authoring` skill

- [ ] 2.1 Create `.agents/skills/skill-authoring/SKILL.md` with the merged body (frontmatter describing both create and update triggers; §"Detect mode" step; create and update branches; the existing guardrails and completion guidance).
- [ ] 2.2 Move (or copy, then delete) the three `references/` notes from `.agents/skills/update-skill/references/` to `.agents/skills/skill-authoring/references/`. (Content is identical between `create-skill` and `update-skill` references; one copy suffices.)
- [ ] 2.3 Verify the new skill folder structure: only `SKILL.md` + `references/`; no legacy subfolders.

## 3. Drop the predecessor skills

- [ ] 3.1 `git rm -r .agents/skills/create-skill/`.
- [ ] 3.2 `git rm -r .agents/skills/update-skill/`.

## 4. Cross-skill reference sweep

- [ ] 4.1 `rg -n 'create-skill|update-skill' .` and fix every hit in the same change.
- [ ] 4.2 Update `docs/skill-sessions/INDEX.md` (generated by the runner; touch only if the generator records the name in a static place).
- [ ] 4.3 Update the in-flight PR #21 description to mention the merged skill.

## 5. Verify

- [ ] 5.1 `openspec validate skill-authoring` after the canonical sync.
- [ ] 5.2 Confirm `find .agents/skills -maxdepth 2 -type d` lists only the three spec-recognised subfolders (or none) plus the new `skill-authoring` folder.
- [ ] 5.3 Confirm the staged CVE scan still passes for the new files.

## 6. Commits (per the §9 grouping rule)

- [ ] 6.1 `Add skill-authoring skill` — proposal, design, new `SKILL.md` + `references/`.
- [ ] 6.2 `Drop create-skill and update-skill` — `git rm` the two predecessor folders.
- [ ] 6.3 `Update cross-skill references` — fix any `rg` hits in docs, comments, and PR body.
- [ ] 6.4 `Push` the new commits to `chore/skill-folder-conventions`; GitHub will update PR #21.

## 7. Archive

- [ ] 7.1 `mv openspec/changes/merge-skill-authoring openspec/changes/archive/2026-08-04-merge-skill-authoring/`.
- [ ] 7.2 Confirm `openspec list --json` returns the expected empty state after the move.
